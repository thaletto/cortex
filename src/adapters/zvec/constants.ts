/**
 * @file @/src/adapters/zvec/constants.ts
 * Shared constants for the zvec adapter.
 */

/** The field name used to store embedding vectors inside a zvec collection. */
export const VECTOR_FIELD = "content_embedding" as const
